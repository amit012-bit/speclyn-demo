# Primary engine (Claude). Used first when present.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Secondary fallback (OpenAI). Used only if the Anthropic key is missing
# or the Claude call fails at runtime.
OPENAI_API_KEY=your_openai_api_key_here
